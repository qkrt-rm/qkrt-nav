map_server:
  ros__parameters:
    use_sim_time: True
    yaml_filename: ""  # Will be overridden in launch file

    # QoS settings to make map visible in RViz
    qos_overrides:
      /map:
        publisher:
          durability: volatile
          reliability: reliable
